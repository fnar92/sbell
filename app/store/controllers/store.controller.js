(function () {     'use strict';        angular	.module('app.home')	.controller('StoreController', StoreController);    function StoreController ($state, $mdDialog, $mdToast, $http, RestService, AuthenticationService, Constants, StoreService) {        /* jshint validthis: true */        console.log('init store');        var scope = this;                       scope.car=[];                                for (var i = 0; i < 2; i++) {            var p={                "id":i+1,                "cantidad": 2,                "imagen": "imagen",                "precio": 200+i+1            };                        scope.car.push(p);        }                scope.total=scope.car.length;                   }})();